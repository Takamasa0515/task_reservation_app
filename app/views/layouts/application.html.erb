<!DOCTYPE html>
<html>
  <head>
    <title>TaskApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
		<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.4/css/all.css">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
		<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
		<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <%= javascript_importmap_tags %>
  </head>

  <body>
		<header class = "header">
			<div class = "header-left-nav">
				<a href="<%= root_path %>"><img class = "header-left-nav-logo", src="https://rails-02-sample.herokuapp.com/assets/common/logo.png"></a>
				<div class = "header-search d-lg-inline-block d-none ">
					<%= form_tag(area_search_path, method: :get) do %>
						<%= text_field_tag :area_search, "", {placeholder: "エリア", class: "form-control"}%><i class = "fas fa-map-marker-alt "></i>
						<%= submit_tag '', class: "d-none"%>
					<% end %>
				</div>
				<div class = "header-search d-lg-inline-block d-none">
					<%= form_tag(keyword_search_path, method: :get) do %>
						<%= text_field_tag :keyword_search, "", placeholder: "キーワード", class: "form-control"%><i class = "fas fa-search "></i>
						<%= submit_tag '', class: "d-none"%>
					<% end %>
				</div>
			</div>
			<div class = "header-right-nav">
				<% if user_signed_in? %>
					<%= render 'layouts/login_user_nav'%>
				<% else %>
					<%= render 'layouts/no_login_user_nav'%>
				<% end %>
			</div>
		</header>
		<div class = "clearfix"></div>
		<%= render 'layouts/notifications' %>
			<%= yield %>
		<footer class = "footer">
			<p>© 2020 POTEPAN.INC.</p>
		</footer>
  </body>
</html>